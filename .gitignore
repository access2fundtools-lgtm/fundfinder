deck_grid.jpg
ff_badge.png
FundFinder-Pitch-Deck.pdf
SYNC-LOG-*.txt
SOCIAL-LOG-*.txt
~$*
*.tmp
_removed/
*.zip
ad_portrait.png
ad_square.png
FACEBOOK-AD-CAMPAIGN.md
WHATSAPP-SETUP.md
FundFinder-Pitch-Deck.pptx
ff_logo_wordmark.png

QA-REPORT-*.txt

# Internal working files & private business documents — never publish to the live site
execution-board.html
check-tasks.mjs
A2F-*.docx
AFAN_S_*.DOC
fundfinder-flag.png
data/backfill-preview.json
*.DOC

# QA hardening 2026-07-29: confidential A2F corporate records and sibling site
# projects live in this folder untracked. Widened so an accidental `git add -A`
# can never stage them into the public fundfinder repo.
A2F-*
a2f_partners*
a2f partners logo.png
a2fpartners-site/
axis-site/

# Deploy-bot 2026-07-30: private contact export (PII) + bulky unreferenced CAC
# source files. The strike-off checker reads cac-data/*.json, not these.
contacts.csv
CAC-Batch6-100k-StrikeOff-List.pdf
cac-strikeoff-batch6.json

# Rendered reels are build outputs, not source
reels/
# QA bot 2026-08-02: local working files that must never be staged by a
# `git add -A` during a deploy sync. markdowntohtml.com.html in particular
# would be picked up by scripts/gen-sitemap.js and published to sitemap.xml.
markdowntohtml.com.html
AKEJU-POST*.md
FACEBOOK-POSTS-*.md
LSETF-outreach-email.md
HEALTH-CHECK-*.md
*-run-in-supabase.sql
EXPORT-*.sql

# Outreach cohort reports contain user contact data — never commit
outreach/

# QA bot scratch dir — never commit
.qa-tmp/

# transfer artifacts — patches shared between sessions
*.patch

# Project Sentry — confidential government proposals + a stray Sentry.io SDK dump.
# The repo root is the public web root for fundfinder.ng and the daily scraper runs
# `git add .`, so anything here would auto-publish. Never commit.
project sentry/

# FarmID project — internal, must never publish to public web root
farmid/

# QA bot 2026-08-14: internal deal/outreach working documents found untracked in
# the public web root. The daily scraper runs `git add .`, so these would have
# auto-published on the next run. Never commit.
AICAN-*.md
OUTREACH-DRAFTS-*.md
PRE-SEND-CHECKLIST.md
Receivables-Term-Sheet-Template.md

# files the mount cannot delete; parked here so git add -A never republishes them
.trash/

# OneUp internal application working files — company facts, eligibility notes and
# draft pitch copy. Repo root is the public web root, so these must never publish.
oneup/

# Internal group matching engine — reveals which grants A2F's own companies
# qualify for. Competitive intelligence; repo root is the public web root.
matching/
